@{
    if (IsPost)
    {
        Response.SetStatus(HttpStatusCode.NotFound);
        return;
    }
    var image = Task_11.PL.WEBLogic.ImageBL.GetByID(Request["Id"]);

    Response.WriteBinary(image.Data, "image/png");
}